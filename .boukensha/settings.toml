[tasks.player]
provider = "anthropic"        # provider name (string)
model = "claude-haiku-4-5"

[tasks.player.prompt_override]
system = true

[mud]
host = "localhost"
port = 4000
username = "dummy"
password = "helloworld"
